/**
 * Fonts
 * @description: Fonts => load all fonts
 */

// ===== fonts ===== //
let uberMove = require("../../assets/fonts/UberMoveMedium.otf");
let uberMoveBold = require("../../assets/fonts/UberMoveBold.otf");

let fonts = {
  uberMove,
  uberMoveBold,
};

export default fonts;
